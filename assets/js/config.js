/* ============================================================
   F.All Cup II — backend config
   Paste your Supabase project values below. The anon (public) key
   is SAFE to embed here — Row Level Security does the gatekeeping.
   Until these are filled in, the site runs in "public preview" mode:
   the reference pages work, and the auth/builder pages show a
   "backend not connected yet" notice instead of live features.
   ============================================================ */
window.FALLCUP_CONFIG = {
  SUPABASE_URL:      "https://hydnuljkitqcsabqduxi.supabase.co",
  SUPABASE_ANON_KEY: "sb_publishable_YyGUnlFW0RFKcumGytFOrg_IYZz8eJa",
};
